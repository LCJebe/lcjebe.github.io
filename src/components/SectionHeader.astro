---
interface Props {
  /** Optional section number + label, e.g. "01 · NOW". Currently unused. */
  marker?: string;
  /** Heading text */
  title: string;
}

const { title } = Astro.props;
---
<div class="section-head">
  <div>
    <h2>{title}</h2>
  </div>
</div>
